{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build with make",  // This label should match the preLaunchTask in launch.json
            "type": "shell",
            "command": "make",  // Command to run make
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
